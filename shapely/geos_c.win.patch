diff --git setup.py setup.py
index 9cad9b6..0b7791d 100755
--- setup.py
+++ setup.py
@@ -140,7 +140,7 @@ if sys.platform == 'win32':
     )
 
 # Build cython extensions, which require development parameters
-include_dirs = [get_config_var('INCLUDEDIR')]
+include_dirs = []
 library_dirs = []
 libraries = []
 extra_link_args = []
diff --git shapely/libgeos.py shapely/libgeos.py
index 424c5ee..ac3dec5 100644
--- shapely/libgeos.py
+++ shapely/libgeos.py
@@ -182,9 +182,10 @@ elif sys.platform == 'win32':
                 os.path.join(os.path.dirname(__file__), "DLLs"))
             wininst_dlls = os.path.abspath(os.__file__ + "../../../DLLs")
             original_path = os.environ['PATH']
-            os.environ['PATH'] = "%s;%s;%s" % \
-                (egg_dlls, wininst_dlls, original_path)
-            lgeos = CDLL("geos.dll")
+            os.environ['PATH'] = "%s;%s;%s;%s" % \
+                (egg_dlls, wininst_dlls, original_path, os.path.join(sys.prefix, "Library", "bin")
+)
+            lgeos = CDLL("geos_c.dll")
         except (ImportError, WindowsError, OSError):
             raise
 
