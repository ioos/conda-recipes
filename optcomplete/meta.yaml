package:
  name: optcomplete
  version: !!str 1.2.devel

source:
  fn: optcomplete-1.2-devel.tar.gz
  url: https://pypi.python.org/packages/source/o/optcomplete/optcomplete-1.2-devel.tar.gz
  md5: 6392cd0a7154b3394004d5ad9d913d59
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  #preserve_egg_dir: True
  #entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - optcomplete = optcomplete:main
    #
    # Would create an entry point called optcomplete that calls optcomplete.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python

  run:
    - python

test:
  # Python imports
  imports:
    - optcomplete

  #commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: http://furius.ca/optcomplete
  license: BSD
  summary: 'Automatic shell completion support for scripts that use optparse.'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
