{% set version = "1.11.4" %}

package:
    name: gdal
    version: {{ version }}

source:
    fn: gdal-{{ version }}.tar.gz
    url: http://download.osgeo.org/gdal/{{ version }}/gdal-{{ version }}.tar.gz
    md5: e9a966f0f5a88c43da47faf78a865952
    patches:
        - clang.patch  # [osx]
        - prepend-dll.patch  # [win]
        - MSC_VER_1900_snprintf.patch  # [win]
        - install_scripts.patch

build:
    number: 4
    features:
        - vc9  # [win and py27]
        - vc10  # [win and py34]
        - vc14  # [win and py35]

requirements:
    build:
        - python
        - setuptools
        - numpy x.x
        - hdf4  # [not win]
        - hdf5 1.8.15.1
        - geos
        - proj.4
        - xerces-c
        - libnetcdf
        - krb5
        - libtiff
        - libpng
        - openjpeg
        - curl
        - expat
        - zlib  # [not win]
        - jpeg  # [not win]
        - postgresql  # [linux]
        - jasper  # [not win]
        - giflib  # [not win]
        - json-c  # [not win]
        - freexl  # [not win]
        - xz  # [not win]
        - libspatialite  # [linux]
    run:
        - python
        - numpy x.x
        - hdf4  # [not win]
        - hdf5 1.8.15.1
        - geos
        - proj.4
        - xerces-c
        - libnetcdf
        - krb5
        - libtiff
        - libpng
        - openjpeg
        - curl
        - expat
        - zlib  # [not win]
        - jpeg  # [not win]
        - postgresql  # [linux]
        - jasper  # [not win]
        - giflib  # [not win]
        - json-c  # [not win]
        - freexl  # [not win]
        - xz  # [not win]
        - libspatialite  # [linux]
test:
    files:
        - os1_hw.py
        - sites.dbf
        - sites.sbn
        - sites.sbx
        - sites.shp
        - sites.shx
    imports:
        - osr
        - gdal
        - gdalconst
        - osgeo
        - osgeo.ogr
        - osgeo.gdal
        - osgeo._gdal
        - osgeo._gdalconst
        - osgeo._ogr
        - osgeo._osr
    commands:
        - ogrinfo sites.shp
        - gdal_grid --version
        - gdal_rasterize --version
        - gdal_translate --version
        - gdaladdo --version
        - gdalenhance --version
        - gdalwarp --version
        - gdalinfo --formats
        - conda inspect linkages -n _test gdal  # [linux]

about:
    home: http://www.gdal.org/
    license: MIT
    summary: The Geospatial Data Abstraction Library (GDAL)

extra:
  recipe-maintainers:
    - ocefpaf
    - kmuehlbauer
    - gillins
